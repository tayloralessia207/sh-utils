# scripts

tiny shell scripts i keep reusing. not a library, just a scratchpad.

- `install.sh` — bootstrap a fresh linux box with my baseline packages
- `backup.sh`  — rsync snapshot with delete + sane excludes
- `quick.sh`   — dispatcher for one-liners i always google

no dependencies beyond gnu coreutils + rsync. tested on debian and arch.
